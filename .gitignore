# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
*.log

# environment variables
.env
.env.production
.env.local
.env.*.local

# macOS-specific files
.DS_Store

# Windows-specific files
Thumbs.db
Desktop.ini

# editor temp / swap files
*.swp
*.swo
*~
*.bak
*.tmp
.history/

# AI assistant working files (any tool, any name)
.aider*
.cursor/
.copilot/

# broader local-config catch-all
*.local

# jetbrains setting folder
.idea/

# vscode — only exclude personal settings, keep team-shared configs
.vscode/settings.json

# agent working folder and Claude project instructions — internal, not for commit
.claude/
CLAUDE.md

# agent review-staging, not for commit
drafts/

# lessons source material — internal first drafts; polished versions ship in src/pages/learned/
lessons/

# generated PDFs / paper renders (these get baked from drafts/, never live in the source tree)
*.pdf

# OS clutter / dev junk that occasionally appears in repo roots
*.DS_Store
.cache/
coverage/

# secrets safety net — never ship even if an errant file appears
*.pem
*.key
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
credentials.json
.secrets
